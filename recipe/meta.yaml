package:
    name: hello
    version: 0.0.1

source:
    path: ..

requirements:
    host:
        - python
        - numpy
        - meson
        - {{compiler('c')}}
        - {{compiler('fortran')}}
    run:
        - python
        - numpy

test:
    imports:
        - hello_lib
    source_files:
        - test/
    requires:
        - pytest
    commands:
        - pytest
